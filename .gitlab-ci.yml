build-job:
  stage: build
  script:
    - terraform init
    - terraform plan

deploy-job:
  stage: deploy
  script:
    - terraform init
    - terraform apply -auto-approve
    - terraform output
    - terraform output host_id > postgres_db_host_id
    - type postgres_db_host_id
  when: manual